version: 2.1

jobs:
  build:
    docker:
      - image: cimg/rustc 1.60.0
    steps:
      - checkout
      - run: cargo --version
      - run:
          name: Run Tests
          command: "cargo test"